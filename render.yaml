services:
  - type: web
    name: damu-exchange
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot/bot.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
